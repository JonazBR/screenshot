{
    "version": 2,
    "name": "bot",
    "buildCommand": "npx playwright install",
        "builds": [
       { "src": "/api.js", "use": "@vercel/node" }
    ],

    "routes": [
       { "src": "/(.*)", "dest": "/api.js" }
    ]
 }
